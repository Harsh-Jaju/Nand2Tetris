/** 
 * Multiplexor:
 * if (sel = 0) out = a, else out = b
 */

CHIP myMux {
    IN a, b, sel;
    OUT out;

    PARTS:
    
    myNot(in=sel,out=sel1);
    myAnd(a=a,b=sel1,out=c1);
    myAnd(a=b,b=sel,out=c2);
    myOr(a=c1,b=c2,out=out);
}